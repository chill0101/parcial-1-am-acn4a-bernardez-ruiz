<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.react_io">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.React_io">

        <activity android:name=".SequenceGameActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".TapTargetActivity" android:exported="false" />


        <activity
            android:name=".GameMenuActivity"
            android:exported="false" />

        <!-- Juego 1  -->
        <activity
            android:name=".ReactionGameActivity"
            android:exported="false" />

        <!-- Pantalla de "En desarrollo" -->
        <activity android:name=".InDevelopmentActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />


            </intent-filter>
        </activity>
    </application>

</manifest>