<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">


    <corners android:radius="16dp"/>
    <padding android:left="8dp" android:top="8dp" android:right="8dp" android:bottom="8dp"/>
    <size android:minWidth="64dp" android:minHeight="64dp"/>

    <!-- Fondo transparente -->
    <solid android:color="@color/darkerPurple" />

    <!-- Borde con el mismo color o un color complementario a tu button_bg -->
    <stroke
        android:width="2dp"
        android:color="#8A6EDB" /> <!-- Ajusta este color al de tu borde deseado -->

    <!-- Mismas esquinas redondeadas que tu button_bg -->
    <corners android:radius="12dp" />

</shape>
