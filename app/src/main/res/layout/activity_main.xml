<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Fondo animado -->
    <FrameLayout
        android:id="@+id/bgContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/bg1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_loop" />

        <ImageView
            android:id="@+id/bg2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_loop" />
    </FrameLayout>

    <!-- Contenido arriba de todo encima -->
    <LinearLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <!-- Título con efecto neon bastante loco -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp">

            <!-- Capa del borde / glow -->
            <TextView
                android:id="@+id/tvTituloStroke"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="REACT IO"
                android:textSize="52sp"
                android:textStyle="bold"
                android:textColor="#9D00FF"
                android:shadowColor="#B000FF"
                android:shadowDx="0"
                android:shadowDy="0"
                android:shadowRadius="32" />

            <!-- Capa del relleno blanco -->
            <TextView
                android:id="@+id/tvTitulo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="REACT IO"
                android:textSize="52sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:shadowColor="#B000FF"
                android:shadowDx="0"
                android:shadowDy="0"
                android:shadowRadius="24" />
        </FrameLayout>

        <!-- Botoncito -->
        <Button
            android:id="@+id/btnEmpezar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¡Empezá!"
            android:textSize="20sp"
            android:paddingLeft="32dp"
            android:paddingRight="32dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"/>
    </LinearLayout>

</FrameLayout>
